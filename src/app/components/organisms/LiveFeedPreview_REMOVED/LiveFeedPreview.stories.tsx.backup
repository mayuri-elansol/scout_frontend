import type { Meta, StoryObj } from '@storybook/react';
import LiveFeedPreview from './LiveFeedPreview';

const meta: Meta<typeof LiveFeedPreview> = {
  title: 'Components/Organisms/LiveFeedPreview',
  component: LiveFeedPreview,
  parameters: {
    layout: 'padded',
    docs: {
      description: {
        component: 'SCOUT Live Feed Preview component showing camera feed placeholder with status indicators. Used in personnel cards and camera monitoring interfaces.',
      },
    },
  },
  tags: ['autodocs'],
  argTypes: {
    status: {
      control: 'select',
      options: ['live', 'offline', 'loading'],
      description: 'Feed status',
    },
    width: {
      control: 'text',
      description: 'Preview width (CSS value)',
    },
    height: {
      control: 'text',
      description: 'Preview height (CSS value)',
    },
    text: {
      control: 'text',
      description: 'Custom preview text',
    },
    showStatus: {
      control: 'boolean',
      description: 'Show live status indicator dot',
    },
    onClick: {
      action: 'preview-clicked',
      description: 'Click handler for opening feed',
    },
  },
};

export default meta;
type Story = StoryObj<typeof meta>;

export const LiveFeed: Story = {
  args: {
    status: 'live',
    text: 'Live Feed Preview',
    showStatus: true,
  },
  parameters: {
    docs: {
      description: {
        story: 'Live camera feed preview with animated status indicator.',
      },
    },
  },
};

export const OfflineFeed: Story = {
  args: {
    status: 'offline',
    showStatus: true,
  },
  parameters: {
    docs: {
      description: {
        story: 'Offline camera feed showing unavailable state.',
      },
    },
  },
};

export const LoadingFeed: Story = {
  args: {
    status: 'loading',
    showStatus: true,
  },
  parameters: {
    docs: {
      description: {
        story: 'Loading camera feed with shimmer animation effect.',
      },
    },
  },
};

export const SmallPreview: Story = {
  args: {
    status: 'live',
    width: '120px',
    height: '80px',
    text: 'Live Feed Preview',
  },
  parameters: {
    docs: {
      description: {
        story: 'Small preview for compact cards and interfaces.',
      },
    },
  },
};

export const LargePreview: Story = {
  args: {
    status: 'live',
    width: '300px',
    height: '200px',
    text: 'Live Feed Preview',
  },
  parameters: {
    docs: {
      description: {
        story: 'Large preview for detailed monitoring interfaces.',
      },
    },
  },
};

export const ClickablePreview: Story = {
  args: {
    status: 'live',
    text: 'Click to View Live Feed',
    showStatus: true,
  },
  parameters: {
    docs: {
      description: {
        story: 'Clickable preview that opens full camera feed.',
      },
    },
  },
};

export const AllStates: Story = {
  render: () => (
    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: '16px' }}>
      <div>
        <h4 style={{ margin: '0 0 8px 0', fontSize: '14px' }}>Live Feed</h4>
        <LiveFeedPreview
          status="live"
          text="Live Feed Preview"
          height="120px"
        />
      </div>
      <div>
        <h4 style={{ margin: '0 0 8px 0', fontSize: '14px' }}>Offline Feed</h4>
        <LiveFeedPreview
          status="offline"
          height="120px"
        />
      </div>
      <div>
        <h4 style={{ margin: '0 0 8px 0', fontSize: '14px' }}>Loading Feed</h4>
        <LiveFeedPreview
          status="loading"
          height="120px"
        />
      </div>
    </div>
  ),
  parameters: {
    docs: {
      description: {
        story: 'All feed preview states shown together for comparison.',
      },
    },
  },
};